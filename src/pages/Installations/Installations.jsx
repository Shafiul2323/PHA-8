import React from 'react';
import { useLoaderData } from 'react-router';

const Installations = () => {
    

    return (
        <div>
            <h1>This is Installations Page</h1>
        </div>
    );
};

export default Installations;